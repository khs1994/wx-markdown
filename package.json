{
  "name": "wx-markdown-next",
  "version": "0.0.1",
  "miniprogram": "markdown",
  "dependencies": {
    "towxml": "docker-practice/towxml"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^2.10.0"
  },
  "scripts": {
    "build": "npx tsc",
    "prepublishOnly": "npm run build",
    "fmt": "npx prettier --write markdown/component/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/docker-practice/wx-markdown-next.git"
  }
}
