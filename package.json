{
  "name": "wx-markdown",
  "version": "0.0.6",
  "miniprogram": "markdown",
  "dependencies": {
    "@pcit/towxml": "0.0.2"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^2.9.0"
  },
  "scripts": {
    "build": "npx tsc",
    "prepublishOnly": "npm run build",
    "fmt": "npx prettier --write markdown/component/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/docker-practice/wx-markdown.git"
  }
}
